# Tasks: Investment Intel AI Agents — POC

**Input**: `specs/001-investment-intel-poc/plan.md`, `spec.md`  
**Prerequisites**: `plan.md` ✅, `spec.md` ✅

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel with other tasks in the same phase
- **[Story]**: US1 = Signal Strategy Config, US2 = Telegram Notifications, US3 = Daily Digest,
  US4 = Telegram Linking, US5 = Alert History, US6 = Anomaly Explanation, US7 = Market Sentiment Pulse,
  US0 = Cross-cutting / Auth / Billing / Admin

---

## Phase 1: Project Setup & Shared Infrastructure

**Purpose**: Establish repo structure, CI, design system, and all shared config before any
feature work begins. No user story can be built without this foundation.

- [ ] T001 Initialise monorepo directory structure per `plan.md` and [architecture-domain-decoupling.md](./architecture-domain-decoupling.md): top-level dirs `backend/`, `ai-service/`, `agent-gateway/`, `frontend/`, `infra/`, `migrations/`, `config/`, `contracts/`; within `backend/` create `cmd/server/`, `platform/` (with `auth/` (+ `auth/supabase/`), `billing/` (+ `billing/stripe/`), `notification/` (+ `notification/telegram/`), `eventbus/` (+ `eventbus/nats/`), `health/`, `user/`, `admin/`, `server/`, `config/`), `domain/investment/` (with `strategies/`, `signals/`, `alerts/`, `watchlist/`, `marketdata/`, `config/`), `pkg/` (with `httputil/`, `validate/`, `testutil/`), `tests/platform/`, `tests/domain/investment/`; within `ai-service/src/` create `platform/` (with `compute/`, `enrichment/`, `content/`, `llm/` (+ `llm/anthropic/`, `llm/openai/`, `llm/gemini/`)), `domain/investment/` (with `indicators/`, `news/`, `enrichment/`, `projects/`); within `frontend/src/` create `platform/` (with `components/`, `pages/auth/`, `pages/settings/`, `pages/billing/`, `pages/admin/`, `hooks/`, `services/`, `lib/`), `domain/investment/` (with `pages/dashboard/`, `pages/strategies/`, `pages/alerts/`, `pages/watchlist/`, `components/`, `services/`); within `agent-gateway/goclaw/agents/` create `_platform/`, `digest-agent/skills/`, `anomaly-agent/skills/`, `sentiment-agent/skills/`; within `config/` create `domain/investment/`. **Provider subdirectories** (`auth/supabase/`, `billing/stripe/`, `notification/telegram/`, `eventbus/nats/`, `llm/anthropic/`, `llm/openai/`, `llm/gemini/`) establish the provider abstraction pattern from day one — each contains only provider-specific implementation code; interfaces live in the parent directory
- [ ] T001a [P] Create `CHANGELOG.md` stub at repository root with an `[Unreleased]` section per constitution Quality Gates §6; all feature PRs MUST append entries incrementally (not batch at release)
- [ ] T002 [P] Bootstrap Go module in `backend/` (`go.mod`, `go.sum`, `golangci-lint` config, `Makefile`); the module must support `platform/` and `domain/investment/` package hierarchy per [architecture-domain-decoupling.md](./architecture-domain-decoupling.md); create stub `platform/auth/interfaces.go` defining `AuthProvider` interface (`ValidateToken`, `CreateUser`, `ResetPassword`, `PublicKey`) — provider-agnostic, no Supabase/Casdoor knowledge; create stub `platform/billing/interfaces.go` defining `BillingProvider` interface (`CreateCheckoutSession`, `ValidateWebhook`, `GetSubscriptionStatus`, `CancelSubscription`), `SubscriptionChecker` interface (`HasActiveSubscription`), and `BillingEventHandler` interface — provider-agnostic, no Stripe knowledge; create stub `platform/notification/interfaces.go` defining `NotificationPayload` struct, `Sender` interface (`Send`, `Channel`), `AccountLinker` interface (`GenerateLink`, `HandleCallback`, `ConfirmLink`, `UnlinkAccount`, `Channel`), and `Dispatcher` interface (`Dispatch`, `RegisterSender`) — provider-agnostic, no Telegram/Discord knowledge; create stub `platform/eventbus/interfaces.go` defining `Publisher` interface (`Publish`), `Consumer` interface (`Subscribe`, `Start`, `Drain`), and `Handler` interface — provider-agnostic, no NATS/Kafka knowledge; create stub `platform/server/interfaces.go` defining `DomainModule` interface (`RegisterRoutes`, `RegisterEventHandlers`, `RegisterBackgroundWorkers`, `Shutdown`); these interfaces establish **two abstraction layers**: (1) platform/domain contract and (2) provider abstraction within platform — **rule: platform interface files MUST NOT import provider subpackages; platform middleware/handlers MUST depend only on interfaces; only `cmd/server/main.go` wires concrete providers**
- [ ] T003 [P] Bootstrap Python project in `ai-service/` (`pyproject.toml`, `uv.lock`, `ruff` config)
- [ ] T053a [P] Scaffold Python ai-service app in `ai-service/src/`: create `main.py` (FastAPI app factory, structlog JSON logging, CORS restricted to internal network), `config.py` (pydantic-settings loading `CRYPTOPANIC_TOKEN`, `REDIS_URL`, `ALLOWED_HOSTS`); expose `GET /health` (liveness) and `GET /health/ready` (readiness — checks Redis connectivity and CryptoPanic reachability); add to `docker-compose.yml` with `--network internal` so it is unreachable from the public internet (Traefik does NOT expose ai-service externally — only the Agent Gateway and Go backend call it internally)
- [ ] T004 [P] Bootstrap React project in `frontend/` (Vite + React 19, TailwindCSS v4, TanStack Router, TanStack Query, Vitest, Playwright, Storybook 8 with `@storybook/react-vite`); configure `.storybook/main.ts` with Vite builder and TailwindCSS; verify `npm run storybook` starts successfully
- [ ] T005 [P] Configure GitHub Actions CI pipeline — create the following workflow files under `.github/workflows/`:

  **`ci-go.yml`** — triggers on `pull_request` and `push` to `main`; jobs:
  - `lint`: `golangci-lint run ./...` (use `golangci-lint-action`)
  - `test`: `go test ./... -race -coverprofile=coverage.out`; fail if coverage < 80% globally or < 95% on `domain/investment/signals/`, `domain/investment/alerts/`, `platform/auth/` (including `platform/auth/supabase/`), `platform/billing/` (including `platform/billing/stripe/`)
  - `build`: `go build ./cmd/server/` to catch compile errors on every PR

  **`ci-python.yml`** — triggers on `pull_request` and `push` to `main`; jobs:
  - `lint`: `ruff check .` and `ruff format --check .`
  - `test`: `pytest --cov=src --cov-fail-under=80` inside `ai-service/`

  **`ci-frontend.yml`** — triggers on `pull_request` and `push` to `main`; jobs:
  - `lint`: `eslint . --max-warnings 0` + `tsc --noEmit`
  - `test`: `vitest run --coverage` inside `frontend/`
  - `storybook`: `npm run build-storybook` + `@storybook/test-runner` interaction tests (see T008a); archive Storybook build as a CI artefact
  - `e2e` (Phase 10 gate, skipped until T073): `playwright test` — kept as a separate job so it can be enabled without touching other jobs

  **`ci-migrations.yml`** — triggers on `pull_request` when files under `migrations/` change; jobs:
  - `validate`: spin up Postgres 18 via service container, run `golang-migrate up` against it, assert exit 0

  All workflows MUST use pinned action versions (e.g., `actions/checkout@v4`) and cache Go modules, pip, and npm dependencies for faster runs
- [ ] T006 [P] Set up `docker-compose.yml` for local dev: Postgres 18 + pgvector, Redis 7, NATS JetStream, Traefik, Agent Gateway (GoClaw `latest` image — web UI embedded, dashboard at `http://localhost:18790`), backend, ai-service, frontend, `prometheus-nats-exporter` — **no separate `agent-gateway-web` container** (Apr 2026: web UI is compiled into the Go binary); NATS service MUST mount a named volume (`nats-data:/data`) and enable JetStream with `FileStorage` configured (`--js --sd /data` flags or equivalent NATS config file) so that stream state persists across `docker compose restart`; **define two explicit Docker networks**: `public` (Traefik, frontend, backend) and `internal` (backend, ai-service, Agent Gateway, NATS, Redis, Postgres) — ai-service MUST be on `internal` only (unreachable from the public internet; Traefik does NOT route to ai-service)
- [ ] T007 [P] Create initial PostgreSQL migration framework (`golang-migrate`); add `migrations/` directory with `001_init.sql` creating base schema namespaces (`app_`, `gc_`)
- [ ] T008 [P] Define TailwindCSS design tokens (colours, spacing, typography) and create shared component primitives: `Button`, `Input`, `Card`, `Badge`, `Spinner`, `EmptyState`, `ErrorMessage`; each primitive MUST have a co-located `*.stories.tsx` file covering all variants (size, state, disabled, loading); Storybook stories serve as the living design-system documentation and visual baseline — **no primitive ships without a story**
- [ ] T008a [P] Add `storybook:build` step to GitHub Actions CI: build static Storybook (`npm run build-storybook`) and run `@storybook/test-runner` to execute interaction tests; fail CI if any story throws or any interaction test fails; Storybook build artefact archived as a CI artefact for visual review
- [ ] T009 [P] Configure Traefik static config: TLS termination (Cloudflare origin cert), routing rules for `api.`, `app.`, `agent.` subdomains, rate-limit middleware
- [ ] T009a [P] Configure and integration-test Traefik rate-limit middleware per NFR-SEC-004: authenticated users ≤ 120 req/min, unauthenticated paths (login, register) ≤ 20 req/min; integration test MUST assert that requests exceeding the limit receive HTTP 429
- [ ] T010 [P] Configure the Agent Gateway (GoClaw default): `.env` with `AGENT_GATEWAY_LLM_API_KEY`, `AGENT_GATEWAY_LLM_PROVIDER=anthropic`, `AGENT_GATEWAY_LLM_MODEL=claude-3-5-haiku-latest`, `AGENT_GATEWAY_TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}` (same single bot — see plan.md §Single Telegram Bot Architecture), `AGENT_GATEWAY_DB_*`; `docker-compose.agent-gateway.yml`; verify `GET /health` returns 200 — use the GoClaw `latest` image (web UI + API in one binary, dashboard at `http://localhost:18790`); use **channel health diagnostics panel** in the Agent Gateway dashboard to verify Telegram bot token and channel connectivity before writing any custom diagnostic code (Apr 2026)
- [ ] T010a [P] Create `specs/001-investment-intel-poc/contracts/rest-api.md`: draft endpoint signatures and response schemas for auth, strategies, alerts, watchlist, billing, and internal service-to-service routes; **MUST include the standardised error response envelope** (see plan.md §Standardised Error Response Contract): `{ error: { code, message, details? } }` — all contract tests MUST validate error responses against this schema; **MUST also include ai-service endpoint contracts**: `POST /indicators/{asset}` (request: OHLCV array + `candle_minutes`; response: indicator struct), `POST /pct_change/{asset}` (request: OHLCV array + `window_minutes`; response: `{ pct_change, quote_currency }`), `POST /explain/{asset}` (request: `{ signal_type, threshold, current_value, operator, news_items: list[NewsItem], price_stats }`, response: `{ explanation: string }`; 8 s timeout, ≤ 150 output tokens — per FR-029/FR-033), `GET /news/{slug}` (response: `list[NewsItem]`), `POST /enrich/news` (request: `list[NewsItem]`; response: `list[EnrichedNewsItem]`), `GET /projects` (response: project list with `quote_currency`), `GET /health`, `GET /health/ready`; **MUST also include internal service-to-service endpoint contracts**: `GET /internal/users?has_watchlist=true` (response: list of users with active watchlists; used by sentiment agent T101), `GET /internal/watchlist?user_id={id}` (response: user's watchlist entries; used by digest agent T056 and sentiment agent T101), `POST /internal/sentiment` (request: `{ user_id, score, confidence, summary, news_item_count }`; used by sentiment agent T101), `PATCH /internal/alerts/:id/explanation` (request: `{ explanation: string }`; used by anomaly agent T093) — all internal endpoints require `AGENT_GATEWAY_INTERNAL_TOKEN` static bearer auth (defined in T057a) — per FR-024 the `NewsProvider` interface contract MUST be defined here before implementation; prerequisite for all contract tests in Phases 3–8
- [ ] T010b [P] Create `specs/001-investment-intel-poc/contracts/nats-events.md`: define the complete NATS JetStream topology — stream configs, subject schemas, consumer configs, and payload contracts; prerequisite for T014, T041, T042, and T042a. Document the following:
  **Stream topology** (two streams — different retention semantics require separate streams):
  | Stream | Subjects | Retention | MaxAge | Storage | Replicas |
  |---|---|---|---|---|---|
  | `SIGNALS` | `signal.triggered.>` | `WorkQueuePolicy` (auto-delete on ACK) | 1 h (safety net for unACKed messages) | `FileStorage` | 1 (POC single-node) |
  | `ALERT_QUEUE` | `alert.pending`, `alert.expired` | `LimitsPolicy` | 25 h (covers FR-025 24 h window + buffer) | `FileStorage` | 1 |
  **Consumer configs** (document alongside each stream):
  - `alerts-dispatcher` on `SIGNALS`: `DeliverAll`, `AckExplicit`, `MaxAckPending: 100` (flow-control ceiling; prevents dispatcher goroutine overload if Telegram API degrades), `AckWait: 30s`
  - `alerts-redriver` on `ALERT_QUEUE` subject `alert.pending`: `DeliverAll`, `AckExplicit`, `MaxDeliver: 48` (≈ 48 × 30 min backoff = 24 h coverage), `AckWait: 60s`; messages that exceed `MaxDeliver` are auto-forwarded by NATS to `alert.expired` subject (Dead Letter) — no application re-publish required
  **Message schemas** (include `schema_version: "v1"` header on all messages for forward compatibility):
  - `signal.triggered.{asset}` — `{ schema_version, user_id, strategy_id, signal_rule_id, asset, trigger_value, threshold, triggered_at }`; subject is asset-keyed; user isolation enforced by `user_id` in payload, **not** by subject
  - `alert.pending` — `{ schema_version, alert_id, user_id, triggered_at }`; produced by `alerts-dispatcher` when `telegram_chat_id IS NULL`
  - `alert.expired` — `{ schema_version, alert_id, user_id, triggered_at, expired_at }`; written by NATS Dead Letter forwarding when `MaxDeliver` is exhausted; consumed by a lightweight handler that sets `telegram_status=Expired` in Postgres
  - ~~`digest.requested`~~ — **removed**: Agent Gateway digest is triggered by its own internal cron scheduler; no NATS producer or consumer exists for this subject
- [ ] T010c [P] Create `specs/001-investment-intel-poc/research.md` with initial structure: Agent Gateway integration notes, market data feed evaluation, crypto news API evaluation, performance test results placeholder (filled during Phase 10)
- [ ] T084 [P] Create `config/seed.yaml` with all POC business configuration: signal type definitions (6 types with parameter schemas, allowed values, defaults, data source routing, candle multipliers), system tunables (`poll_interval_seconds: 30`, `signal_cooldown_seconds: 300`, `indicator_cache_ttl_seconds: 25`, `ai_service_readiness_timeout_seconds: 60`), and `projects_seed` entries (BTC + ETH as signal assets, ~18 watchlist-only projects); create `config/seed.schema.json` (JSON Schema draft-2020-12) validating the seed file structure; the schema MUST enforce: `signal_types` keys match a `^[a-z_]+$` pattern, each signal type has `display_name`, `parameters` map with typed entries, valid `data_source` enum; `projects_seed` entries have `slug`, `display_name`, `symbol`, `coingecko_id`, `quote_currency`, `is_signal_asset`; unit test: load `seed.yaml`, validate against `seed.schema.json`, assert zero errors
- [ ] T085 [P] Create `contracts/strategy-definition.schema.json` (JSON Schema draft-2020-12): Strategy Definition Format (SDF) with discriminated union on `signal_type`; top-level properties: `name` (string, 1-100 chars), `asset` (string, project slug), `status` (enum: active/paused, optional — defaults to active), `rules` (array, minItems 1); each rule variant (`price_threshold_rule`, `pct_change_rule`, `rsi_rule`, `volume_spike_rule`, `macd_crossover_rule`, `bollinger_breach_rule`) specifies exactly the parameters valid for that signal type with `additionalProperties: false`; allowed values for `window_minutes`, `candle_minutes`, defaults, etc. MUST match `config/seed.yaml` definitions; unit test: validate 6 example SDF documents (one per signal type) and 4 invalid documents (missing required field, unknown signal type, invalid `window_minutes`, extra properties) against the schema
- [ ] T086 [P] Implement seed config loader in `backend/domain/investment/config/seed.go`: parse `config/domain/investment/seed.yaml` into typed Go structs at startup; validate against `config/seed.schema.json` using `santhosh-tekuri/jsonschema` (or `xeipuuv/gojsonschema`); expose `SeedConfig` singleton (signal type registry, project seed list, system tunables); the signal evaluator, strategy validator, and project seeder all read from this singleton — no hardcoded allowed-value sets in application code; fail backend startup with a descriptive error if validation fails; unit test: valid seed loads, invalid seed (missing required field) panics at startup

**Checkpoint**: All services start locally via `docker compose up`. CI runs and is green on an empty test suite. Seed config and SDF schema validated.

---

## Phase 1b: LLM Provider Abstraction & Langfuse Observability

**Purpose**: Establish the provider-agnostic LLM interface and LLM-specific observability before any AI feature work begins. This mirrors the provider-abstraction pattern already established for auth (Supabase), billing (Stripe), notification (Telegram), and event bus (NATS). All LLM-consuming features (digest, anomaly explanation, sentiment pulse) depend on this foundation.

**Dependencies**: Phase 1 (T053a ai-service scaffold is now in Phase 1).

### Tests — LLM Provider (write first)

- [ ] T110 [P] Unit tests for `LLMProvider` interface compliance in `ai-service/tests/platform/llm/test_interfaces.py`: verify `LLMProvider` ABC enforces `complete()`, `complete_json()`, `model_name`, `provider_name`; verify `LLMResponse` dataclass contains `text`, `input_tokens`, `output_tokens`, `model`, `provider`; verify `LLMConfig` validates required fields (`provider`, `model`, `api_key`)
- [ ] T111 [P] Unit tests for LLM provider factory in `ai-service/tests/platform/llm/test_factory.py`: `LLM_PROVIDER=anthropic` → `AnthropicProvider` instance; `LLM_PROVIDER=openai` → `OpenAIProvider` instance; `LLM_PROVIDER=gemini` → `GeminiProvider` instance; unknown provider → raises `ValueError` with descriptive message; missing SDK for configured provider → raises `ImportError` with install instruction
- [ ] T112 [P] Unit tests for Anthropic provider in `ai-service/tests/platform/llm/test_anthropic.py`: mock `httpx` responses; happy path → `LLMResponse` with correct token counts; API error → raises `LLMProviderError`; timeout → raises `LLMTimeoutError`; `complete_json()` with valid JSON → parsed dict; `complete_json()` with invalid JSON → retries once then raises `LLMResponseError`

### Implementation — LLM Provider

- [ ] T113 Implement `LLMProvider` interface in `ai-service/src/platform/llm/interfaces.py`: `LLMProvider` ABC with `complete(*, system, user, max_tokens, temperature, response_format) -> LLMResponse`, `complete_json(*, system, user, max_tokens, json_schema) -> dict`, `model_name` property, `provider_name` property; `LLMResponse` dataclass (`text: str`, `input_tokens: int`, `output_tokens: int`, `model: str`, `provider: str`, `latency_ms: float`); `LLMConfig` pydantic model (`provider: str`, `model: str`, `api_key: str`, `base_url: str | None`, `max_retries: int = 2`, `timeout_seconds: float = 30`); exception hierarchy: `LLMError` (base), `LLMProviderError`, `LLMTimeoutError`, `LLMResponseError`, `LLMConfigError`
- [ ] T114 [P] Implement LLM provider factory in `ai-service/src/platform/llm/factory.py`: `create_llm_provider(config: LLMConfig) -> LLMProvider`; registry dict mapping provider names to module paths `{"anthropic": "platform.llm.anthropic.client:AnthropicProvider", "openai": "platform.llm.openai.client:OpenAIProvider", "gemini": "platform.llm.gemini.client:GeminiProvider"}`; dynamic import via `importlib`; raise `LLMConfigError` on unknown provider; raise clear `ImportError` with `pip install anthropic` / `pip install openai` / `pip install google-genai` message if SDK missing; singleton pattern — same config returns same provider instance
- [ ] T115 Implement Anthropic provider in `ai-service/src/platform/llm/anthropic/client.py`: `AnthropicProvider(LLMProvider)` — uses `anthropic` SDK (`AsyncAnthropic` client); `complete()` maps to `messages.create()`; `complete_json()` adds `response_format={"type": "json_object"}` (or parses JSON from text for models that don't support native JSON mode); config in `anthropic/config.py`; all calls wrapped with Langfuse `@observe` decorator (T118); retry on transient errors (429, 500, 503) up to `max_retries` with exponential backoff; `api_key` read from `LLM_API_KEY` env var
- [ ] T115a [P] Implement OpenAI provider in `ai-service/src/platform/llm/openai/client.py`: `OpenAIProvider(LLMProvider)` — uses `openai` SDK (`AsyncOpenAI` client); `complete()` maps to `chat.completions.create()`; `complete_json()` uses `response_format={"type": "json_object"}`; config in `openai/config.py`; supports `LLM_BASE_URL` override for Azure OpenAI and OpenAI-compatible APIs (vLLM, Ollama, etc.); retry + Langfuse instrumentation same as T115
- [ ] T115b [P] Implement Gemini provider in `ai-service/src/platform/llm/gemini/client.py`: `GeminiProvider(LLMProvider)` — uses `google-genai` SDK; `complete()` maps to `generate_content()`; `complete_json()` uses `response_mime_type="application/json"` + `response_schema`; config in `gemini/config.py`; retry + Langfuse instrumentation same as T115
- [ ] T116 Wire LLM provider into ai-service startup in `ai-service/src/main.py`: read `LLM_PROVIDER`, `LLM_MODEL`, `LLM_API_KEY`, `LLM_BASE_URL`, `LLM_MAX_RETRIES`, `LLM_TIMEOUT_SECONDS` from env via `config.py` (pydantic-settings); call `create_llm_provider()` at app startup; inject provider into FastAPI dependency injection (`app.state.llm_provider`); fail startup with descriptive error if LLM config is invalid or SDK is missing
- [ ] T117 [P] Add `LLM_PROVIDER=anthropic`, `LLM_MODEL=claude-3-5-haiku-latest`, `LLM_API_KEY` to `ai-service` service in `docker-compose.yml` and to `config/platform.env.example`; add `AGENT_GATEWAY_LLM_PROVIDER=anthropic`, `AGENT_GATEWAY_LLM_MODEL=claude-3-5-haiku-latest` to Agent Gateway `.env` files; add `OTEL_EXPORTER_OTLP_ENDPOINT=http://langfuse-web:3100/api/public/otel` and `OTEL_EXPORTER_OTLP_HEADERS` (Basic Auth with Langfuse keys) to Agent Gateway `.env` files for OTLP→Langfuse LLM trace export

### Tests — Langfuse (write first)

- [ ] T118 [P] Unit tests for Langfuse integration in `ai-service/tests/platform/llm/test_langfuse.py`: verify `@observe` decorator is applied to `LLMProvider.complete()` and `complete_json()` calls; mock Langfuse client; assert trace contains: `model`, `input_tokens`, `output_tokens`, `latency_ms`, `user_id` (when provided), `skill_name` (when provided); verify `LANGFUSE_ENABLED=false` → decorator is a no-op

### Implementation — Langfuse

- [ ] T119 Add Langfuse LLM observability to ai-service: add `langfuse` to `pyproject.toml` dependencies; configure Langfuse client in `ai-service/src/platform/llm/observability.py`: `init_langfuse(public_key, secret_key, host, enabled)` called at app startup; wrap all `LLMProvider` calls with Langfuse `@observe` decorator that logs: generation name (skill), model, provider, input/output tokens, latency, cost (auto-calculated by Langfuse), user_id (passed via context); `LANGFUSE_ENABLED=false` → all decorators are no-ops (zero overhead in tests)
- [ ] T120 [P] Create `infra/docker-compose.langfuse.yml`: `langfuse-web` container (image: `langfuse/langfuse:latest`, port `3100`, env: `DATABASE_URL` pointing to shared Postgres with `langfuse_` schema prefix, `CLICKHOUSE_URL`, `REDIS_URL`, `NEXTAUTH_SECRET`, `SALT`, `LANGFUSE_INIT_ORG_ID`, `LANGFUSE_INIT_PROJECT_NAME=investment-intel`), `langfuse-worker` container (same image, `LANGFUSE_WORKER=true`), `clickhouse` container (image: `clickhouse/clickhouse-server:latest`, volume: `clickhouse-data`); both Langfuse containers on `internal` network; health check on `langfuse-web:3100/api/public/health`; Langfuse OTLP endpoint (`/api/public/otel`) is automatically available — receives traces from both ai-service (Python SDK) and Agent Gateway (OTLP export); document usage: `docker compose -f docker-compose.yml -f infra/docker-compose.langfuse.yml up` (opt-in — not required for core development)
- [ ] T121 [P] Add `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY`, `LANGFUSE_HOST=http://langfuse-web:3100`, `LANGFUSE_ENABLED=true` to ai-service env in docker-compose and `config/platform.env.example`; add Langfuse initial project bootstrap script in `infra/langfuse-init.sh` (creates project + API keys on first run via Langfuse API)
- [ ] T122 Integration test: verify Langfuse receives traces from ai-service LLM calls in `ai-service/tests/integration/test_langfuse.py`: start Langfuse (via docker-compose), make a mock LLM call through `LLMProvider`, assert trace appears in Langfuse API `GET /api/public/traces`; skip if `LANGFUSE_ENABLED=false`
- [ ] T122a [P] Integration test: verify Langfuse receives OTLP traces from Agent Gateway in `agent-gateway/tests/test_langfuse_otlp.sh` (or equivalent): start Langfuse + Agent Gateway (via docker-compose), trigger a digest or test skill that makes an LLM call, assert trace with `gen_ai.request.model` attribute appears in Langfuse API `GET /api/public/traces`; verify `gen_ai.usage.input_tokens` and `gen_ai.usage.output_tokens` are populated; skip if Langfuse is not running

**Checkpoint**: `LLMProvider` interface is implemented with Anthropic (default), OpenAI, and Gemini providers. Factory creates the correct provider from env vars. Langfuse traces LLM calls from both ai-service (Python SDK) and Agent Gateway (OTLP). Switching `LLM_PROVIDER=openai` + `LLM_MODEL=gpt-4o-mini` + `LLM_API_KEY=sk-...` changes the LLM backend with zero code changes. CI green.

---

## Phase 2: Foundation — Auth, Supabase, NATS, Stripe Bootstrap

**Purpose**: Core infrastructure all user stories depend on. No feature work starts until this phase is complete.

**⚠️ CRITICAL**: Stories US1–US5 and billing MUST NOT be started until this phase passes its checkpoint.

- [ ] T011 Write contract tests for `POST /auth/register`, `POST /auth/login`, `POST /auth/logout`, `POST /auth/forgot-password`, `POST /auth/reset-password` in `backend/tests/contract/auth_test.go`
- [ ] T012 Implement auth platform layer in `backend/platform/auth/`: (1) `platform/auth/middleware.go` — JWT validation middleware that uses the `AuthProvider` interface from T002 (provider-agnostic — does NOT import Supabase SDK); (2) `platform/auth/handler.go` — `/auth/*` route handlers that delegate to `AuthProvider` interface; (3) `platform/auth/supabase/client.go` — Supabase-specific implementation of `AuthProvider` (JWT validation using Supabase public key, user creation, password reset delegation to Supabase built-in flows per FR-020); user context injection; email verification + password-reset flows are **delegated to Supabase Auth** — no custom implementation required; **to swap auth provider** (e.g., Casdoor): create `platform/auth/casdoor/client.go` implementing `AuthProvider`, change one line in `cmd/server/main.go` — middleware, handlers, and all domain code remain unchanged
- [ ] T013 [P] Create `app_users` migration (`002_users.sql`): `id`, `email`, `timezone` (default `UTC`), `telegram_chat_id`, `telegram_linked_at`, `subscription_status`, `stripe_customer_id`, `created_at`
- [ ] T013a [P] Create `app_projects` migration (`002a_projects.sql`): `app_projects` table with columns `slug VARCHAR PRIMARY KEY`, `display_name`, `symbol`, `coingecko_id`, `quote_currency VARCHAR(3) NOT NULL DEFAULT 'USD'`, `is_signal_asset BOOLEAN DEFAULT FALSE`; seed BTC (`slug='btc'`, `is_signal_asset=TRUE`, `quote_currency='USD'`) and ETH (`slug='eth'`, `is_signal_asset=TRUE`, `quote_currency='USD'`); seed ~18 watchlist-only projects with `is_signal_asset=FALSE`; this table is the FK target for `app_strategies.asset` (Phase 3 T021) and `app_watchlist_entries.project_slug` (Phase 7 T051) — **MUST run before `003_strategies.sql`**
- [ ] T014 [P] Implement NATS JetStream provider in `backend/platform/eventbus/nats/` (implements `Publisher`, `Consumer` interfaces from T002): `nats/client.go` — NATS JetStream connection, publisher, durable consumer setup, reconnect logic; on service startup the helper MUST call `js.AddStream()` (or `js.UpdateStream()` if already exists) for **both** streams defined in T010b — `SIGNALS` and `ALERT_QUEUE` — with the exact `RetentionPolicy`, `MaxAge`, `Storage`, and `Replicas` values from the contract; do NOT rely on NATS auto-create defaults (wrong retention policy will be silently applied); all NATS-specific code lives inside `eventbus/nats/` — the `eventbus/interfaces.go` and all consumers (domain code) depend only on the provider-agnostic `Publisher`/`Consumer` interfaces; **to swap event bus** (e.g., Kafka): create `platform/eventbus/kafka/` implementing `Publisher` + `Consumer`, change wiring in `main.go`; integration test with local NATS must assert that both streams exist with correct config after `Connect()`
- [ ] T015 [P] Bootstrap billing platform layer in `backend/platform/billing/`: (1) `platform/billing/gate.go` — subscription gate middleware using `SubscriptionChecker` interface from T002 (provider-agnostic — does NOT import Stripe SDK); (2) `platform/billing/stripe/client.go` — Stripe-specific implementation of `BillingProvider` (Stripe Go SDK init, checkout session creation, webhook signature validation + parsing into provider-agnostic `WebhookEvent`); (3) `platform/billing/stripe/webhook.go` — Stripe-specific webhook handler for `customer.subscription.created`, `customer.subscription.deleted`, `invoice.payment_failed`; all Stripe-specific code lives inside `billing/stripe/` — domain and platform middleware depend only on `BillingProvider`/`SubscriptionChecker` interfaces; **to swap billing provider** (e.g., LemonSqueezy): create `platform/billing/lemonsqueezy/` implementing `BillingProvider`, change wiring in `main.go`; contract tests for `POST /billing/webhook`
- [ ] T016 [P] Implement `GET /health` and `GET /ready` endpoints in `backend/platform/health/`; wire into Traefik health checks
- [ ] T017 [P] Implement React auth flow pages: `SignIn`, `SignUp` (with email verification notice), `ForgotPassword`, `ResetPassword`; use TanStack Query mutation hooks; loading + error states required
- [ ] T018 [P] Implement auth route guard in React: redirect unauthenticated users to `/sign-in`; persist JWT in httpOnly cookie via backend proxy
- [ ] T018a [P] Implement `PATCH /users/me` endpoint in `backend/platform/user/handler.go`: accepts partial update of user profile fields (`timezone`, `sentiment_push_enabled`); validates timezone against IANA timezone database; RLS scoped to authenticated user; contract test in `backend/tests/contract/user_test.go`; this endpoint is used by the timezone selector (T058a) and the sentiment push toggle (T104)

**Checkpoint**: User can register, verify email, log in, and access a blank authenticated dashboard. CI green.

---

## Phase 3: User Story 1 — Signal Strategy Configuration (P1) 🎯 MVP

**Goal**: Logged-in users can create, edit, activate, pause, and delete signal strategies for any
asset in the curated project list (BTC and ETH seeded for POC) with price-threshold, % price-change,
and RSI signal rules. Dashboard shows all strategies.

**Independent Test**: Create a BTC price-threshold strategy at $70k → save → retrieve → appears
as Active. Edit threshold → verify persisted. Delete → gone. No notifications needed.

### Tests — US1 (write first, must fail before implementation)

- [ ] T019 [P] [US1] Contract tests: `POST /strategies`, `GET /strategies`, `GET /strategies/:id`, `PUT /strategies/:id`, `DELETE /strategies/:id`, `PATCH /strategies/:id/status` in `backend/tests/contract/strategies_test.go`
- [ ] T020 [P] [US1] Unit tests for signal rule validator in `backend/domain/investment/strategies/validator_test.go`: empty rules, contradictory rules (price above X AND below X), missing required fields, **asset with `is_signal_asset=FALSE` rejected** (per FR-002), `window_minutes` required for percentage-change signal type, `window_minutes` value outside allowed set (5, 15, 60, 240, 1440) rejected, `window_minutes` ignored/disallowed for price-threshold and RSI signal types, `candle_minutes` required for RSI signal type, `candle_minutes` value outside allowed set (5, 15, 60) rejected, `candle_minutes` defaults to 15 when omitted for RSI, `candle_minutes` ignored/disallowed for price-threshold and percentage-change signal types, `candle_minutes` required for volume-spike / MACD-crossover / Bollinger-Band-breach signal types (defaults to 15 when omitted), `volume_threshold_pct` required for volume-spike (defaults to 200 when omitted; must be > 0), `cross_direction` required for MACD-crossover (only `bullish` or `bearish` accepted), `band_direction` required for Bollinger-Band-breach (only `upper` or `lower` accepted), unknown signal type rejected

### Implementation — US1

- [ ] T021 [US1] Migration `003_strategies.sql`: `app_strategies` (`id`, `user_id`, `name`, `asset VARCHAR NOT NULL REFERENCES app_projects(slug)`, `status` enum Active/Paused, `created_at`); `app_signal_rules` (`id`, `strategy_id REFERENCES app_strategies(id) ON DELETE CASCADE`, `signal_type` enum {`price_threshold`, `pct_change`, `rsi`, `volume_spike`, `macd_crossover`, `bollinger_breach`}, `operator`, `threshold` NUMERIC — denominated in the asset's `quote_currency` for price-based signals (`price_threshold`, `pct_change`) or dimensionless for indicator signals, `window_minutes`, `candle_minutes`, `volume_threshold_pct`, `cross_direction`, `band_direction`); `window_minutes` MUST have a CHECK constraint restricting values to `{5, 15, 60, 240, 1440}` for `pct_change` signal type and MUST be NULL for all others; `candle_minutes` MUST have a CHECK constraint restricting values to `{5, 15, 60}` for `rsi`, `volume_spike`, `macd_crossover`, `bollinger_breach` signal types with DEFAULT 15, and MUST be NULL for `price_threshold` and `pct_change`; `volume_threshold_pct` MUST have a CHECK > 0 and DEFAULT 200 for `volume_spike`, NULL for all others; `cross_direction` MUST be CHECK `{bullish, bearish}` for `macd_crossover`, NULL for all others; `band_direction` MUST be CHECK `{upper, lower}` for `bollinger_breach`, NULL for all others; **indexes** (per plan.md §Database Index Strategy): `idx_strategies_user_id(user_id)`, `idx_strategies_user_status(user_id, status)`, `idx_signal_rules_strategy_id(strategy_id)`
- [ ] T022 [P] [US1] Implement strategy service in `backend/domain/investment/strategies/service.go`: CRUD, status toggle, signal rule validation (cyclomatic complexity ≤ 10 per function); strategy creation MUST validate `app_projects.is_signal_asset = TRUE` for the selected asset and reject with a clear error if `FALSE` (per FR-002)
- [ ] T023 [P] [US1] Implement strategy HTTP handlers in `backend/domain/investment/strategies/handler.go`; wire into router via `DomainModule.RegisterRoutes()`; RLS enforced via Supabase JWT user ID
- [ ] T024 [US1] React — Strategy list page (`/dashboard`): fetches `GET /strategies`, shows Active/Paused badges, empty-state, loading skeleton, error state
- [ ] T025 [P] [US1] React — Strategy create/edit form (`/strategies/new`, `/strategies/:id/edit`): asset selector (populated from `GET /projects` — curated project list **filtered to `is_signal_asset=TRUE` entries only**; not a hardcoded BTC/ETH dropdown), signal type selector (options: Price Threshold, % Price Change, RSI, Volume Spike, MACD Crossover, Bollinger Band Breach), operator + threshold inputs (threshold field MUST show the asset's `quote_currency` symbol — e.g., `$` for USD — as a prefix label; the currency symbol is derived from the selected asset's `quote_currency` in the `GET /projects` response), **time-window dropdown** (visible only when signal type = percentage change; options: 5 min, 15 min, 1 h, 4 h, 24 h mapping to `window_minutes` values 5, 15, 60, 240, 1440), **candle-size dropdown** (visible when signal type ∈ {RSI, Volume Spike, MACD Crossover, Bollinger Band Breach}; options: 5 min, 15 min, 1 h mapping to `candle_minutes` values 5, 15, 60; default = 15 min; label: "Candle Timeframe"), **volume threshold input** (visible only when signal type = Volume Spike; numeric input for `volume_threshold_pct`; default 200; label: "Volume Spike % of SMA"), **cross direction selector** (visible only when signal type = MACD Crossover; options: Bullish / Bearish), **band direction selector** (visible only when signal type = Bollinger Band Breach; options: Upper / Lower), client-side validation matching server rules
- [ ] T026 [P] [US1] React — Strategy status toggle (Activate/Pause button) with optimistic update + rollback on error
- [ ] T027 [US1] Integration test: full strategy lifecycle (create → activate → edit → pause → delete) against live Postgres in `backend/tests/integration/strategies_test.go`
- [ ] T087 [P] [US1] Implement SDF-based validation in `backend/domain/investment/strategies/validator.go`: validate incoming `POST /strategies` and `PUT /strategies/:id` request bodies against `contracts/strategy-definition.schema.json` **before** business-rule checks; the schema validation catches structural errors (unknown signal type, wrong parameter types, out-of-range values); business-rule validation (is_signal_asset check, contradictory rules) runs second; error responses MUST distinguish schema violations (`code: SCHEMA_VALIDATION_FAILED`) from business-rule violations (`code: VALIDATION_FAILED`) per the standardised error envelope; unit test: schema-invalid request returns 400 with schema error details, schema-valid but business-invalid request returns 400 with business error details
- [ ] T088 [P] [US1] Contract tests for `POST /strategies/import` and `GET /strategies/:id/export` in `backend/tests/contract/strategies_import_export_test.go`: import single SDF → 201 with created strategy; import batch of 3 SDFs → 201 with 3 strategies; import with one invalid SDF in batch → 400 (entire batch rejected, no partial creation); export returns pure SDF matching schema; round-trip: create via import → export → re-import produces identical strategy
- [ ] T089 [P] [US1] Implement import/export handlers in `backend/domain/investment/strategies/import_handler.go`: `POST /strategies/import` accepts `{ "strategies": [SDF, ...] }`, validates each against schema + business rules, creates all in a single DB transaction (atomic — all succeed or all fail); `GET /strategies/:id/export` returns the strategy as a pure SDF document (no server metadata except `id`); wire into router; RLS enforced via Supabase JWT user ID

**Checkpoint**: US1 fully functional and independently testable. Strategy import/export operational. CI green. ≥ 80% coverage on `domain/investment/strategies/`.

---

## Phase 4: User Story 4 — Telegram Account Linking (P1)

**Goal**: User links their Telegram account via self-service flow; confirmation message sent; can unlink; broken links detected.

**Independent Test**: Follow in-app instructions → complete Telegram bot `/start` → account marked linked → confirmation Telegram message received.

### Tests — US4 (write first)

- [ ] T028 [P] [US4] Contract tests: `POST /telegram/link/initiate`, `POST /telegram/link/confirm`, `DELETE /telegram/link`, `GET /telegram/link/status` in `backend/tests/contract/telegram_test.go`

### Implementation — US4

- [ ] T029 [US4] Migration `004_telegram.sql`: add `telegram_link_token` (short-lived UUID), `telegram_link_token_expires_at` to `app_users`
- [ ] T030 [P] [US4] Implement Telegram notification sender in `backend/platform/notification/telegram/sender.go` (implements `Sender` interface from T002): `Send(ctx, NotificationPayload)` resolves `RecipientUserID` to `telegram_chat_id` and calls Telegram Bot API `sendMessage`; `Channel()` returns `"telegram"`; also implement `telegram/handler.go` for `/telegram/webhook` routes; bot token read from `TELEGRAM_BOT_TOKEN` env var (single bot shared with the Agent Gateway — see plan.md §Single Telegram Bot Architecture); register webhook at `https://api.<domain>/telegram/webhook`; webhook handler MUST filter updates to only process `/start` commands for account linking (all other inbound messages are ignored for POC); **to add Discord as a notification channel**: create `platform/notification/discord/sender.go` implementing `Sender` + register with `Dispatcher` — zero changes to existing Telegram code or domain code
- [ ] T031 [P] [US4] Implement Telegram account linking in `backend/platform/notification/telegram/linker.go` (implements `AccountLinker` interface from T002): `GenerateLink` creates one-time link token + bot deep-link URL, `HandleCallback` processes `/start?token=` deep-link webhook, `ConfirmLink` persists `telegram_chat_id` + sends confirmation message, `UnlinkAccount` sets `telegram_chat_id = NULL`; detect broken link (Telegram 403/blocked → call `UnlinkAccount`, emit web-app re-link prompt); `Channel()` returns `"telegram"`; **to add Discord linking**: create `platform/notification/discord/linker.go` implementing `AccountLinker` with Discord OAuth flow — zero changes to existing code
- [ ] T032 [US4] Implement link/unlink HTTP handlers; register Telegram webhook endpoint with Telegram Bot API
- [ ] T033 [P] [US4] React — Settings page (`/settings/telegram`): shows link status, displays bot deep-link + instructions when unlinked, shows linked username + unlink button when linked, re-link prompt when broken
- [ ] T034 [US4] Integration test: link flow, unlink, broken-link detection in `backend/tests/integration/telegram_test.go`; MUST explicitly assert the Telegram 403/blocked → `telegram_chat_id = NULL` state transition and that a re-link prompt flag is set (per FR-019)

**Checkpoint**: US4 fully functional. User can link and unlink. CI green.

---

## Phase 5: User Story 2 — Real-Time Telegram Notifications (P2)

**Goal**: When an active strategy's signal condition fires, user receives a Telegram notification within 60 s.

**Independent Test**: With active BTC strategy + linked Telegram, simulate signal trigger → Telegram message delivered < 60 s with correct content. Paused strategy → no message. Two users → isolated.

### Tests — US2 (write first)

- [ ] T035 [P] [US2] Unit tests for signal evaluator in `backend/domain/investment/signals/evaluator_test.go`: price threshold above/below, % change across multiple `window_minutes` values (5 min and 1440 min at minimum — 5 min verifies CryptoCompare → ai-service `pct_change` path; 1440 min verifies CoinGecko `price_change_percentage_24h` path), RSI overbought/oversold across multiple `candle_minutes` values (5 min and 60 min at minimum — verifies correct candle count is fetched: 70 vs. 840 1-min candles), volume spike above threshold (current vol / 20-period SMA > `volume_threshold_pct` / 100) and below threshold across `candle_minutes` values (verifies 300 vs. 1200 1-min candles fetched for 15 vs. 60), MACD crossover bullish and bearish (verifies MACD line vs. signal line cross detection, 525 vs. 2100 1-min candles for 15 vs. 60), Bollinger Band upper breach and lower breach across `candle_minutes` values (verifies close > upper band and close < lower band, 300 vs. 1200 1-min candles for 15 vs. 60); deterministic with mocked market data; MUST include a panic-recovery test (per NFR-REL-001): inject a panic in one evaluation tick and assert that the goroutine survives and the next tick executes successfully
- [ ] T036 [P] [US2] Contract test: `POST /signals/test-trigger` (admin-only test endpoint for CI) in `backend/tests/contract/signals_test.go`
- [ ] T037 [P] [US2] Unit tests for alert dispatcher in `backend/domain/investment/alerts/dispatcher_test.go`: correct message format, paused strategy = no send, retry logic (3× exponential back-off), idempotency guard — duplicate `signal_rule_id + triggered_at` MUST NOT create a second Alert record (per NFR-REL-002)

### Implementation — US2

- [ ] T038 [US2] Migration `005_alerts.sql`: `app_alerts` (`id`, `strategy_id REFERENCES app_strategies(id) ON DELETE SET NULL`, `user_id`, `signal_rule_id`, `asset`, `trigger_value`, `threshold`, `quote_currency VARCHAR(3) NOT NULL DEFAULT 'USD'`, `triggered_at`, `telegram_status` enum Pending/Sent/Failed, `retry_count`, `explanation TEXT` — nullable; populated by anomaly explanation enrichment step (US6/FR-032); NULL when explanation is unavailable or skipped) — `strategy_id ON DELETE SET NULL` ensures alert history is preserved when a strategy is deleted (per FR-001 deletion cascade rules); `quote_currency` is snapshotted from `app_projects` at alert creation time so historical alerts remain accurate even if the project's quote currency is changed later; **indexes** (per plan.md §Database Index Strategy): `idx_alerts_user_created(user_id, triggered_at DESC)`, `idx_alerts_user_strategy(user_id, strategy_id, triggered_at DESC)`, **UNIQUE** `idx_alerts_idempotency(signal_rule_id, triggered_at)` — enforces NFR-REL-002, `idx_alerts_pending_status(telegram_status) WHERE telegram_status IN ('Pending', 'Failed')` — partial index for re-drive worker and dashboard queries
- [ ] T039 [US2] Implement market data adapter interface `domain/investment/marketdata/interfaces.go` and CoinGecko implementation `domain/investment/marketdata/coingecko.go` (domain concern — investment-specific data sources); CoinGecko serves two purposes: (a) spot price for price-threshold signals, (b) `price_change_percentage_24h` for 1440-min % change signals; the adapter interface MUST accept `window_minutes` so the evaluator can route to the correct provider; the CoinGecko adapter MUST resolve the strategy asset slug (e.g., `btc`) to a CoinGecko coin ID (e.g., `bitcoin`) and quote currency (e.g., `usd`) via `app_projects.coingecko_id` and `app_projects.quote_currency` — load the slug→{coin-ID, quote_currency} map on startup and refresh every 5 min (or expose via ai-service `GET /projects`); **note**: a newly-added project in `app_projects` may take up to 5 min to be recognised by the evaluator due to this cache refresh interval; CoinGecko calls MUST pass `vs_currencies={quote_currency}` (default `usd`); **HTTP 429 handling**: on CoinGecko 429 (rate limit exceeded), adapter MUST retry with exponential backoff (initial 1 s, max 3 retries); on persistent 429, skip the signal evaluation for that tick (log warning, do NOT fire alert on error) and emit Prometheus counter `market_data_rate_limited_total{provider="coingecko"}`; unit tests with mocked HTTP responses MUST include a 429 retry scenario
- [ ] T039a [P] [US2] Implement CryptoCompare `histominute` adapter in `domain/investment/marketdata/cryptocompare.go` for intraday OHLCV data; MUST pass `tsym={quote_currency}` (from `app_projects.quote_currency`, default `USD`) on all API calls; serves multiple purposes: (a) `14 × candle_minutes` 1-min candles for RSI, (b) `20 × candle_minutes` 1-min candles for volume spike and Bollinger Bands, (c) `35 × candle_minutes` 1-min candles for MACD (**note: `candle_minutes=60` → 2100 candles, which exceeds CryptoCompare's 2000/call limit — adapter MUST use two paginated calls**: first call `limit=2000`, second call for remaining 100 candles using `toTs` set to earliest timestamp from first batch; reducing warm-up to 33 candles is NOT acceptable — MACD signal line requires all 35 candles for numerical stability), (d) `window_minutes` 1-min candles for intraday % price change signals (`window_minutes` ∈ {5, 15, 60, 240}) — Go fetches the OHLCV slice but does NOT compute % change; the slice is forwarded to ai-service which returns the computed `pct_change` value; the adapter accepts a generic `candle_count` parameter so the evaluator can request the right number per signal type; Go does NOT compute any indicators or OHLCV-derived math directly; **HTTP 429 handling**: on CryptoCompare 429 (rate limit exceeded), adapter MUST retry with exponential backoff (initial 1 s, max 3 retries); on persistent 429, skip the signal evaluation for that tick (log warning) and emit Prometheus counter `market_data_rate_limited_total{provider="cryptocompare"}`; unit tests with mocked HTTP covering RSI, volume spike, MACD, Bollinger Band, and % change paths at different candle sizes; **MUST include a test for MACD at `candle_minutes=60` verifying two paginated API calls are made and the resulting 2100 candles are correctly concatenated**; **MUST include a test for 429 retry and eventual skip**
- [ ] T040 [P] [US2] Implement technical indicators endpoint in `ai-service/src/domain/investment/indicators/`: `rsi.py` (14-period RSI using `pandas-ta`; accepts `candle_minutes` parameter — resamples incoming 1-min OHLCV slice into candles of the requested size via `pandas.DataFrame.resample()` before computing RSI-14), `volume_spike.py` (20-period volume SMA via `pandas-ta.sma()`; resamples 1-min candles into requested candle size; returns `volume_spike_ratio` = current volume / SMA; threshold comparison happens in Go evaluator), `macd.py` (MACD(12,26,9) via `pandas-ta.macd()`; resamples 1-min candles; returns `macd_line`, `macd_signal`, `macd_histogram` for the latest candle; cross detection happens in Go evaluator by comparing current and previous candle values), `bollinger.py` (Bollinger Bands(20,2) via `pandas-ta.bbands()`; resamples 1-min candles; returns `bb_upper`, `bb_mid`, `bb_lower` and latest `close` for the most recent candle; breach detection happens in Go evaluator), `price_stats.py` (24 h open/high/low/close/pct_change), `pct_change.py` (intraday % price change: accepts the OHLCV slice, extracts first close and last close, computes `(last − first) / first × 100`; used for `window_minutes` ∈ {5, 15, 60, 240} — keeps all OHLCV math in Python; Go evaluator compares the returned `pct_change` against the user's threshold), `cache.py` (two cache key patterns — **indicator cache**: `indicators:{asset}:{candle_minutes}` TTL = 25 s for RSI/volume/MACD/Bollinger; **pct_change cache**: `pct_change:{asset}:{window_minutes}` TTL = 25 s for intraday % change — keyed by `window_minutes` because OHLCV slice sizes differ from indicator slices), `router.py` (`POST /indicators/{asset}` — accepts OHLCV slice + `candle_minutes` query param, returns `{ rsi, volume_spike_ratio, macd_line, macd_signal, macd_histogram, bb_upper, bb_mid, bb_lower, price_stats, quote_currency }`, checks indicator cache first; **`POST /pct_change/{asset}`** — accepts OHLCV slice + `window_minutes` query param, returns `{ pct_change, quote_currency }`, checks pct_change cache first — separate endpoint to avoid cache key collision with indicator cache); unit tests covering: RSI overbought (>70) / oversold (<30) / neutral, volume spike ratio above and below threshold, MACD bullish and bearish cross (compare consecutive candles), Bollinger upper and lower breach, intraday pct_change positive and negative across different window sizes, cache-hit path, and correct resampling for `candle_minutes` 5 vs. 60
- [ ] T040a [P] [US2] Update Go signal evaluator (`domain/investment/signals/evaluator.go`) for indicator-based and OHLCV-derived signal types: read `candle_minutes` (for indicator signals) or `window_minutes` (for intraday % change) from the signal rule; determine candle count by signal type (RSI: `14 × candle_minutes`, volume spike / Bollinger: `20 × candle_minutes`, MACD: `35 × candle_minutes`, intraday % change: `window_minutes` 1-min candles); fetch the required 1-min candles via CryptoCompare adapter; **indicator signals** (RSI, volume spike, MACD, Bollinger): `POST` the slice to `ai-service/indicators/{asset}?candle_minutes={candle_minutes}`; **intraday % change** (`window_minutes` ≤ 240): `POST` the slice to `ai-service/pct_change/{asset}?window_minutes={window_minutes}` (separate endpoint and cache key — see plan.md §Indicator cache design); parse the response and evaluate the relevant field per signal type: RSI → compare `rsi` against overbought/oversold threshold; volume spike → compare `volume_spike_ratio` against `volume_threshold_pct / 100`; MACD crossover → detect `macd_line` crossing `macd_signal` in the configured `cross_direction` (requires comparing current and previous candle values from the response); Bollinger Band breach → compare latest `close` against `bb_upper` or `bb_lower` per `band_direction`; **intraday % change** → compare `pct_change` from ai-service response against the user's threshold (Go does NOT compute the percentage itself — ai-service owns all OHLCV math); **circuit breaker** (`sony/gobreaker`): wrap all ai-service HTTP calls in a circuit breaker (open after 5 consecutive failures, half-open probe after 30 s); when circuit is open, skip indicator/pct_change evaluation for that tick (log warning, do NOT fire alert on error); price-threshold and CoinGecko 24h % change signals are NOT affected by the circuit breaker (they don't call ai-service); cache indicator responses in Redis at `indicators:{asset}:{candle_minutes}` and pct_change responses at `pct_change:{asset}:{window_minutes}`, both with TTL = 25 s; unit tests MUST cover: RSI signal path with `candle_minutes=15` and `candle_minutes=60`, volume spike fire and no-fire, MACD bullish cross and bearish cross, Bollinger upper breach and lower breach, **intraday pct_change fire and no-fire for `window_minutes=15` and `window_minutes=240`**, indicator cache-hit skips HTTP call, **pct_change cache-hit skips HTTP call (separate cache key from indicators)**, different `candle_minutes` values use separate cache keys, **circuit breaker open → tick skipped with warning log**, **circuit breaker half-open → probe succeeds → circuit closes**, ai-service timeout falls back to skipping the tick (log + continue, do NOT fire alert on error)
- [ ] T041 [US2] Implement signal evaluation loop in `backend/domain/investment/signals/poller.go`: 30 s Go ticker, single-pass evaluation across all active strategies per tick, publish `signal.triggered.{asset}` to NATS JetStream on match; payload MUST include `user_id`, `strategy_id`, `signal_rule_id`, `asset`, `trigger_value`, `threshold`, `triggered_at`; subject is asset-keyed (`signal.triggered.btc` / `signal.triggered.eth`) — user isolation is enforced in the dispatcher by `user_id` in the payload, not by subject; **signal cooldown**: before publishing, check Redis key `cooldown:{signal_rule_id}` (TTL = configurable, default 5 min via `SIGNAL_COOLDOWN_SECONDS` env var); if key exists, skip publish for this tick (condition is still true but already fired recently); if key absent, publish and set the key with TTL — this prevents duplicate NATS events when a condition remains true across multiple consecutive ticks and protects against alert spam; **ai-service readiness gate**: on startup, poll `GET ai-service:8000/health/ready` with 2 s interval and 60 s overall timeout before starting the 30 s ticker; if ai-service is not ready within 60 s, start the ticker but log CRITICAL warning and skip all indicator-based + pct_change evaluations (price-threshold + CoinGecko 24h % change still evaluate normally); **graceful shutdown**: on `SIGTERM` / `SIGINT`, (1) stop the 30 s ticker (no new ticks), (2) drain NATS connections (finish in-flight ACKs, close consumers + publisher), (3) close Redis pool, (4) close Postgres pool, (5) `http.Server.Shutdown(ctx)` with 15 s deadline; log each step; force-close on deadline exceeded; unit tests MUST cover: fires on first match, suppressed on second tick within cooldown, fires again after cooldown expires, **graceful shutdown stops ticker and drains NATS before closing pools**
- [ ] T042 [US2] Implement NATS consumer + alert dispatcher in `backend/domain/investment/alerts/dispatcher.go`: consume `signal.triggered.{asset}` via platform `EventConsumer` interface (durable consumer group `alerts-dispatcher`), persist Alert record scoped to `user_id`, format `NotificationPayload` and dispatch via platform `notification.Dispatcher` (domain formats the message, platform delivers it), update `telegram_status`; if user has no linked Telegram (`telegram_chat_id IS NULL`), set `telegram_status=Pending` and publish `alert.pending` to NATS (payload: `alert_id`, `user_id`, `triggered_at`) instead of attempting delivery; 3× retry with exponential back-off on Telegram API failure; surface `telegram_status=Failed` for web-app indicator (per FR-010, FR-025)
- [ ] T042a [P] [US2] Implement NATS consumer + re-drive worker in `backend/domain/investment/alerts/redriver.go`: consume `alert.pending` from the `ALERT_QUEUE` stream via platform `EventConsumer` interface (durable consumer `alerts-redriver`); on receipt, check if user has now linked Telegram; if linked, attempt `telegram.bot.SendMessage` and update `telegram_status=Sent`, then ACK the message; if still unlinked, call `msg.NakWithDelay(backoff)` — **do NOT re-publish a new message** (re-publishing creates stream bloat, duplicate-message risk, and false Prometheus pending-count alerts); backoff schedule: 1 min, 5 min, 15 min, 30 min, then 30 min for all remaining retries up to `MaxDeliver: 48`; when NATS exhausts `MaxDeliver` it auto-forwards the message to `alert.expired` subject — a separate lightweight handler in `redriver.go` consumes `alert.expired` and sets `telegram_status=Expired` in Postgres (no Telegram call); idempotency: check `telegram_status` in Postgres before attempting delivery — if already `Sent`, ACK without sending; unit tests in `backend/domain/investment/alerts/redriver_test.go` MUST cover: deliver-on-link, NakWithDelay-when-unlinked, expired-DLQ-handler sets correct DB status, idempotent double-delivery
- [ ] T043 [P] [US2] Integration test: end-to-end signal trigger → NATS → alert persist → Telegram send (with mocked Telegram Bot API) in `backend/tests/integration/signal_notification_test.go`; MUST include the following cases: (a) happy path — linked user receives message; (b) durable consumer restart — message is redelivered and idempotency guard (`signal_rule_id + triggered_at`) prevents duplicate `Alert` record; (c) unlinked user — `alert.pending` is published and no Telegram call is made; (d) re-drive after linking — re-drive worker delivers queued alert within SLA
- [ ] T043a [P] [US2] React — Delivery-failure indicator on strategy dashboard card (`/dashboard`): when any alert for a strategy has `telegram_status=Failed`, show a red badge on the card; clicking it navigates to alert history filtered by that strategy (per FR-010); Vitest unit test for the badge component
- [ ] T043b [P] [US2] React — Delay-warning banner on dashboard: when any alert has `telegram_status=Pending` and `triggered_at` is older than 5 minutes, display an amber warning banner per FR-025; auto-dismiss when all pending alerts resolve; Vitest unit test

**Checkpoint**: Signal fires → Telegram notification < 60 s. Paused strategy = silent. Two-user isolation verified. CI green. ≥ 95% coverage on `domain/investment/signals/` and `domain/investment/alerts/`.

---

## Phase 6: User Story 5 — Alert History (P2)

**Goal**: Read-only alert history per user in the web app, filterable by strategy, reverse-chronological.

**Independent Test**: Strategy with 3 past alerts → open history page → all 3 listed with correct metadata → filter by strategy → only matching alerts shown → empty state when none.

### Tests — US5 (write first)

- [ ] T044 [P] [US5] Contract tests: `GET /alerts` (paginated, filter by `strategy_id`), `GET /alerts/:id` in `backend/tests/contract/alerts_test.go`

### Implementation — US5

- [ ] T045 [US5] Implement alert query handler in `backend/domain/investment/alerts/handler.go`: `GET /alerts` with `strategy_id` filter, pagination (limit/offset), RLS scoped to authenticated user; enforce `limit` max 100 per FR-022 — clamp values > 100 to 100; default page size = 20
- [ ] T046 [P] [US5] React — Alert history page (`/alerts`): reverse-chronological list, strategy filter dropdown, pagination, loading skeleton, empty state, delivery-failure badge; `GET /alerts` via TanStack Query
- [ ] T046a [P] [US5] React — Per-row delivery-failure badge in alert history: each alert row MUST show a coloured status badge (`Sent` = green, `Pending` = amber, `Failed` = red, `Expired` = grey) per FR-010; Vitest unit test for badge states
- [ ] T047 [US5] Integration test: alert list + filter in `backend/tests/integration/alerts_test.go`

**Checkpoint**: US5 functional. History visible after US2 generates alerts. CI green.

---

## Phase 7: User Story 3 — Watchlist & Daily Digest (P3)

**Goal**: User manages a watchlist; Agent Gateway digest agent (GoClaw default) runs daily at 08:30 UTC, fetches news via CryptoPanic, summarises with the configured LLM provider (POC default: Anthropic claude-3-5-haiku), sends one Telegram message per user with per-project sections.

**Independent Test**: Two projects on watchlist → next digest run → single Telegram message with one section per project. Empty watchlist → no message. New project added before run → included.

### Tests — US3 (write first)

- [ ] T048 [P] [US3] Contract tests: `POST /watchlist`, `GET /watchlist`, `DELETE /watchlist/:project_id` in `backend/tests/contract/watchlist_test.go`
- [ ] T049 [P] [US3] Unit tests for CryptoPanic adapter in `ai-service/tests/unit/test_news_adapter.py`: response parsing, quota-exceeded fallback, empty results
- [ ] T050 [P] [US3] Agent Gateway digest skill test: validate `crypto-digest.md` skill prompt produces correctly structured output with mocked HTTP fetch response (manual verification + prompt regression test)

### Implementation — US3

- [ ] T051 [US3] Migration `006_watchlist.sql`: `app_watchlist_entries` (`id`, `user_id`, `project_slug REFERENCES app_projects(slug)`, `project_name`, `added_at`); **indexes**: `idx_watchlist_user_id(user_id)`; **note**: the `app_projects` table (FK target) is created in Phase 2 by T013a (`002a_projects.sql`) — this migration depends on it
- [ ] T052 [P] [US3] Implement watchlist CRUD handlers in `backend/domain/investment/watchlist/handler.go`; contract: add/remove/list entries; RLS scoped to user
- [ ] T053 [P] [US3] React — Watchlist page (`/watchlist`): grid of curated projects with add/remove toggle, loading, empty-state ("add projects to receive your daily digest"), error state
- [ ] T053b [P] [US3] Implement Redis-backed daily quota counter in `ai-service/src/news/quota.py`: `QuotaTracker` class with `increment(token) -> bool` (returns `False` when limit reached); key `cryptopanic:quota:{token}:{YYYY-MM-DD}`, TTL = 25 h; unit tests covering: under-limit returns True, at-limit returns False, key expires next day
- [ ] T053c [P] [US3] Implement curated project registry in `ai-service/src/projects/registry.py`: **DB-backed** registry reading from `app_projects` table via async Postgres connection (ai-service connects to the shared Postgres instance in read-only mode); exposes `GET /projects` endpoint returning the full list including `slug`, `display_name`, `symbol`, `coingecko_id`, `quote_currency`, and `is_signal_asset` (response MUST include `quote_currency` and `is_signal_asset` per project so the React asset selector can display the correct currency symbol and filter signal-eligible assets, and the Go evaluator can pass quote_currency to market data adapters); results are cached in-memory with 5 min TTL (adding a new project in `app_projects` is visible within 5 min without code deploy); used by React watchlist page, strategy create/edit form, and Agent Gateway skill to resolve slugs; unit test the registry mapping completeness against a test database fixture (all ~20 POC projects present)
- [ ] T054 [US3] Implement CryptoPanic news adapter in `ai-service/src/news/cryptopanic.py`: fetch news by currency slug, parse items (title, url, published_at), DuckDuckGo fallback on quota exceeded; expose `GET /news/{project_slug}` endpoint; wire `QuotaTracker` from T053b to gate CryptoPanic calls; **implement DuckDuckGo fallback adapter** in `ai-service/src/news/duckduckgo.py`: `DuckDuckGoAdapter` implements `NewsProvider` interface; uses `httpx` to query DuckDuckGo search API with `{project_name} crypto news` query; parse top 5 results into `NewsItem` format; activated automatically when `QuotaTracker` returns `False`; unit tests for DuckDuckGo response parsing and fallback trigger logic
- [ ] T054a [P] [US3] Define abstract `NewsProvider` interface in `ai-service/src/news/provider.py`: `fetch_news(project_slug: str) -> list[NewsItem]` with `NewsItem` dataclass (title, url, published_at, source); `CryptoPanicAdapter` in T054 MUST implement this interface; unit tests asserting interface contract; interface contract also documented in `contracts/rest-api.md`
- [ ] T054b [P] [US3] Implement news enrichment endpoint in `ai-service/src/enrichment/`: `sentiment.py` (VADER sentiment scoring on news headlines — `compound` score per item), `router.py` (`POST /enrich/news` — accepts `list[NewsItem]`, returns `list[EnrichedNewsItem]` with `sentiment_score` and `is_duplicate` flag based on URL hash deduplication); Agent Gateway digest skill calls this before passing items to the LLM — shorter prompt, higher summary quality; unit tests for sentiment scoring, deduplication logic, and empty-list handling
- [ ] T055 [US3] Create Agent Gateway digest agent in `agent-gateway/goclaw/agents/digest-agent/`: `AGENT.md` (persona: "Investment Intel Digest Bot"; instructions to fetch each user's watchlist via backend API, call news endpoint per project, summarise with LLM, send via `message` tool); `HEARTBEAT.md` (confirm digest sent) — **note (Apr 2026)**: Knowledge Graph sharing is configured separately from workspace sharing in the Agent Gateway agent setup UI; if KG access is required for the digest agent, enable KG sharing independently
- [ ] T056 [P] [US3] Create Agent Gateway `crypto-digest` skill in `agent-gateway/goclaw/agents/digest-agent/skills/crypto-digest.md`: skill steps — (1) `GET /watchlist` for user via internal service token (see T057a), (2) HTTP fetch CryptoPanic per project, (3) LLM summarise (configured model, ≤ 200 tokens/project), (4) `message` to Telegram chat ID; skill MUST explicitly handle the no-news case: if HTTP fetch returns zero items for a project, insert a section reading "No significant updates in the last 24 hours" (per FR-015) — **note (Apr 2026)**: parallel HTTP fetch across watchlist projects is reliable; use subagent `waitAll` for concurrent news fetch to reduce total digest latency; auto-retry and token tracking are built in
- [ ] T056a [P] [US3] Wire price-movement data into Agent Gateway digest skill (FR-014): the digest agent MUST call `POST /indicators/{asset}` (or a dedicated `GET /price_stats/{asset}` endpoint on ai-service) per watchlisted project to retrieve 24 h OHLCV summary (open, high, low, close, pct_change); include the price-movement section alongside news in each project's digest section; unit test the skill template output includes both news and price data
- [ ] T057 [US3] Configure Agent Gateway cron: add cron entry in agent config `cron: "30 8 * * *"` (08:30 UTC); verify trigger via Agent Gateway web dashboard — **note (Apr 2026)**: timezone handling is now stable for all schedule kinds (`cron`, `at`, `every`); use the **"Run Now"** button in the dashboard to manually trigger the digest agent during development and verification (previously broken, fixed Apr 2026)
- [ ] T057a [P] [US3] Define and implement service-to-service auth for Agent Gateway → backend API calls: `AGENT_GATEWAY_INTERNAL_TOKEN` env var; add `/internal/` route prefix in Traefik config validated by a static-bearer middleware (bypasses Supabase JWT); document token scheme in `contracts/rest-api.md`; unit test the middleware — **note (Apr 2026)**: additionally configure **per-agent grants** in the Agent Gateway (e.g., GoClaw) to scope the digest agent's access to only the `/internal/watchlist` and `/internal/users` endpoints; per-agent grants provide a finer-grained permission layer on top of the static bearer token; additionally scope the anomaly agent to `/internal/alerts/:id/explanation` and the sentiment agent to `/internal/sentiment` and `/internal/users`; if the provider type is changed, the Agent Gateway will automatically re-validate for SSRF
- [ ] T058 [P] [US3] React — Digest history section on watchlist page: last digest timestamp + status (Sent/Pending/Failed); re-link prompt if Telegram not linked
- [ ] T058a [P] [US3] React — Timezone selector in `/settings/account`: dropdown of common IANA timezones; persisted to `app_users.timezone` via `PATCH /users/me`; display hint "Your daily digest is scheduled for 09:00 [timezone]"; include UI notice that digest is currently sent at 08:30 UTC (POC limitation — per-timezone scheduling is post-POC)
- [ ] T059 [US3] Integration test: watchlist CRUD in `backend/tests/integration/watchlist_test.go`; news adapter in `ai-service/tests/integration/test_news_adapter.py`

**Checkpoint**: Watchlist CRUD works. Agent Gateway digest cron fires at 08:30 UTC and delivers Telegram messages. Empty watchlist = no message. CI green.

---

## Phase 7a: User Story 6 — Anomaly Explanation (P3) + User Story 7 — Market Sentiment Pulse (P3)

**Goal**: Enrich fired alerts with LLM-generated explanations of why the signal triggered; provide periodic market sentiment classification stored for trend analysis and optionally pushed to Telegram.

**Dependencies**: Phase 5 (US2 — alerts exist to enrich), Phase 7 (US3 — news adapter + ai-service + Agent Gateway infrastructure in place).

**Independent Test — US6**: Fire a BTC price-threshold signal → Telegram alert arrives with a 1–2 sentence explanation referencing a relevant news headline or market context. Fire the same signal when ai-service is down → alert arrives without explanation (non-blocking).

**Independent Test — US7**: Sentiment pulse cron fires → sentiment score stored in DB with `bullish`/`neutral`/`bearish` classification → user with `sentiment_push_enabled=TRUE` receives a short Telegram message. User with push disabled → score stored, no Telegram.

### Tests — US6 (write first)

- [ ] T090 [P] [US6] Unit tests for anomaly explanation enrichment in `ai-service/tests/unit/test_explanation.py`: happy path (news + market context → explanation string returned), no-news fallback (market-context-only prompt → explanation without news references), LLM timeout → raises `ExplanationTimeoutError`, LLM error → raises `ExplanationError`, empty market context → still produces explanation from news alone
- [ ] T091 [P] [US6] Unit tests for Go alert dispatcher explanation integration in `backend/domain/investment/alerts/dispatcher_test.go` (extend existing): alert with explanation → Telegram message includes explanation text, alert without explanation (timeout) → Telegram message sent without explanation field, explanation persisted on `app_alerts.explanation` column

### Implementation — US6

- [ ] T092 [US6] Implement anomaly explanation endpoint in `ai-service/src/domain/investment/enrichment/explanation.py`: `POST /explain/{asset}` — accepts `{ signal_type, threshold, current_value, operator, news_items: list[NewsItem], price_stats: { open, high, low, close, pct_change } }`, constructs LLM prompt (system: "produce 1–2 sentences explaining why this signal fired; if no news provided, state 'no specific news catalyst identified'"), calls the configured LLM provider via `LLMProvider` interface (≤ 150 output tokens, 8 s timeout), returns `{ explanation: string }`; cache at `explanation:{asset}:{signal_rule_id}:{minute_bucket}` TTL = 55 s; all LLM calls instrumented with Langfuse `@observe` decorator; wire into `ai-service/src/domain/investment/enrichment/router.py`; unit tests for prompt construction, cache hit, error paths, and LLM provider mock
- [ ] T093 [P] [US6] Update Go alert dispatcher (`domain/investment/alerts/dispatcher.go`) to call anomaly explanation enrichment after persisting the Alert record: (1) fetch recent news via `GET ai-service/news/{asset}` (reuse existing adapter, cached), (2) fetch 24 h price stats via `POST ai-service/indicators/{asset}` (reuse existing, cached), (3) call `POST ai-service/explain/{asset}` with signal metadata + news + price stats, (4) on success: update `app_alerts.explanation` with the returned text and append to `NotificationPayload.Body`, (5) on failure (timeout, error): log + increment `anomaly_explanation_failures_total` Prometheus counter, proceed with alert delivery without explanation; entire enrichment step wrapped in 10 s `context.WithTimeout`; unit test: mock ai-service returning explanation → verify `app_alerts.explanation` is set and Telegram message includes it; mock ai-service timeout → verify alert delivered without explanation
- [ ] T094 [P] [US6] React — Display explanation in alert history (`/alerts`): if `alert.explanation` is non-null, show it below the standard alert fields in the detail row; collapsed by default with "Why did this fire?" toggle; Vitest unit test for the toggle component
- [ ] T095 [US6] Integration test in `backend/tests/integration/anomaly_explanation_test.go`: (a) signal fire → explanation enrichment → alert with explanation text persisted + Telegram message includes explanation; (b) ai-service /explain timeout → alert persisted without explanation, Telegram delivered; (c) no news available → explanation uses market-context-only language

### Tests — US7 (write first)

- [ ] T096 [P] [US7] Contract tests: `GET /sentiment` (paginated, authenticated), `POST /internal/sentiment` (internal auth) in `backend/tests/contract/sentiment_test.go`
- [ ] T097 [P] [US7] Agent Gateway sentiment skill test: validate `sentiment-pulse.md` skill prompt produces correctly structured JSON output (`{ score, confidence, summary }`) with mocked HTTP fetch response (manual verification + prompt regression test)

### Implementation — US7

- [ ] T098 [US7] Migration `008_sentiment.sql`: `app_sentiment_scores` (`id UUID PRIMARY KEY`, `user_id REFERENCES app_users(id)`, `score` enum {bullish, neutral, bearish}, `confidence NUMERIC(3,2) CHECK (confidence >= 0 AND confidence <= 1)`, `summary TEXT`, `news_item_count INT`, `scored_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`); **indexes**: `idx_sentiment_user_scored(user_id, scored_at DESC)`; RLS enabled, scoped to `user_id`
- [ ] T098a [US7] Migration `008a_users_sentiment.sql`: add `sentiment_push_enabled BOOLEAN DEFAULT FALSE` column to `app_users`
- [ ] T099 [P] [US7] Implement sentiment CRUD handlers in `backend/domain/investment/sentiment/handler.go`: `POST /internal/sentiment` (internal auth — accepts `{ user_id, score, confidence, summary, news_item_count }`, persists to `app_sentiment_scores`); `GET /api/v1/sentiment?limit=N` (authenticated, RLS scoped, default limit 20, max 100, ordered by `scored_at DESC`); wire into router via `DomainModule.RegisterRoutes()`
- [ ] T100 [P] [US7] Create Agent Gateway sentiment agent in `agent-gateway/goclaw/agents/sentiment-agent/`: `AGENT.md` (persona: "Market Sentiment Analyst"; instructions to fetch users with active watchlists, collect recent news per user's watchlisted projects, classify sentiment via LLM, store score via backend internal API, optionally push to Telegram); `HEARTBEAT.md` (confirm sentiment pulse completed)
- [ ] T101 [P] [US7] Create Agent Gateway `sentiment-pulse` skill in `agent-gateway/goclaw/agents/sentiment-agent/skills/sentiment-pulse.md`: skill steps — (1) `GET /internal/users?has_watchlist=true` to get users with active watchlists, (2) for each user: `GET /internal/watchlist?user_id={id}` → for each project: `GET ai-service/news/{slug}` (parallel via subagent `waitAll`), collect top N items (configurable via `SENTIMENT_NEWS_LIMIT`, default 10), (3) LLM classify sentiment (configured model, ≤ 100 tokens, structured JSON output: `{ "score": "bullish|neutral|bearish", "confidence": 0.0-1.0, "summary": "..." }`), (4) `POST /internal/sentiment` to store score, (5) if user has `sentiment_push_enabled=TRUE`: `message` to Telegram with emoji-prefixed summary (🟢 Bullish / 🟡 Neutral / 🔴 Bearish); skill MUST handle zero-news case: record score as `neutral` with confidence `0.0` and summary "Insufficient news data", skip Telegram push
- [ ] T102 [US7] Configure Agent Gateway sentiment cron: add cron entry in agent config `cron: "0 */4 * * *"` (every 4 h, configurable via `SENTIMENT_PULSE_CRON`); verify trigger via Agent Gateway web dashboard "Run Now" button
- [ ] T103 [P] [US7] React — Sentiment history section on watchlist page (`/watchlist`): latest sentiment score badge (🟢/🟡/🔴), last 5 scores as mini trend sparkline, link to full history; sentiment push toggle (checkbox bound to `PATCH /users/me` with `sentiment_push_enabled`); Vitest unit test for badge and toggle components
- [ ] T104 [P] [US7] React — Settings page update (`/settings/account`): add "Sentiment Push Notifications" toggle under the Telegram section; disabled state if Telegram not linked; Vitest unit test
- [ ] T105 [US7] Integration test: sentiment CRUD in `backend/tests/integration/sentiment_test.go`: (a) `POST /internal/sentiment` stores score, `GET /sentiment` returns it; (b) pagination limit/offset; (c) RLS — user A cannot see user B's scores; (d) `sentiment_push_enabled` toggle via `PATCH /users/me`

**Checkpoint**: US6 — Alerts arrive with LLM-generated explanations; explanation visible in alert history; non-blocking on failure. US7 — Sentiment pulse runs every 4 h; scores stored and queryable; opt-in Telegram push works. CI green.

---

## Phase 8: Billing & Subscription (US0)

**Goal**: Users subscribe via Stripe from the web app; subscription status gates access to signal strategies and digest.

- [ ] T060 Write contract tests for `POST /billing/create-checkout-session`, `GET /billing/subscription-status`, `POST /billing/webhook` in `backend/tests/contract/billing_test.go`
- [ ] T061 [P] Implement Stripe checkout in `backend/platform/billing/stripe/checkout.go` (implements `BillingProvider.CreateCheckoutSession`): create Stripe customer + checkout session, return provider-agnostic `CheckoutSession` struct with redirect URL; all Stripe SDK calls are encapsulated inside `billing/stripe/` — the billing handler in `billing/handler.go` calls only the `BillingProvider` interface
- [ ] T062 [P] Implement Stripe webhook handler in `backend/platform/billing/stripe/webhook.go` (implements `BillingProvider.ValidateWebhook`): validate `Stripe-Signature` header, parse Stripe event into provider-agnostic `WebhookEvent`; the billing event router in `billing/handler.go` maps `WebhookEvent.Type` to `BillingEventHandler` callbacks: `subscription.created` → `OnSubscriptionCreated` (set `subscription_status=active`); `subscription.deleted` → `OnSubscriptionCancelled` (set `subscription_status=cancelled`); `invoice.payment_failed` → `OnPaymentFailed` (set `subscription_status=past_due`); all Stripe-specific signature validation and event parsing is encapsulated in `billing/stripe/`
- [ ] T063 [P] Implement subscription gate middleware in `backend/platform/billing/gate.go`: uses `SubscriptionChecker` interface (provider-agnostic — does NOT import Stripe SDK); routes under `/strategies`, `/watchlist`, `/alerts` return 402 if `subscription_status != active`
- [ ] T064 [P] React — Billing page (`/billing`): current plan status, "Subscribe" CTA (redirects to Stripe Checkout), "Manage subscription" link (Stripe Customer Portal); loading + error states
- [ ] T065 React — Subscription gate in frontend: redirect to `/billing` with explanation banner when API returns 402
- [ ] T066 Integration test: Stripe webhook lifecycle (created → cancelled → payment_failed) using Stripe CLI event fixtures

**Checkpoint**: Subscription required to access features. Stripe webhook correctly updates status. CI green.

---

## Phase 9: Admin Panel (US0)

**Goal**: Admin users can manage all users, view subscription statuses, and configure Stripe products.

- [ ] T067 Migration `007_admin.sql`: add `is_admin` boolean to `app_users`; seed one admin user for POC
- [ ] T068 [P] Implement admin middleware in backend: validate `is_admin = true` from JWT claims; return 403 otherwise
- [ ] T069 [P] Implement admin handlers: `GET /admin/users` (list all users + subscription status), `PATCH /admin/users/:id/subscription` (manually override status)
- [ ] T070 [P] React — Admin page (`/admin`): user table (email, status, Telegram linked, last active), subscription override controls; guarded by admin role check

**Checkpoint**: Admin can view and manage all users. Non-admins receive 403. CI green.

---

## Phase 10: Observability, Performance & Quality Gates

**Purpose**: Wire monitoring, validate performance budgets, finalise CI gates.

- [ ] T071 [P] Configure Prometheus scrape targets: backend `/metrics` (Go runtime + custom `signal_evaluations_total`, `alerts_dispatched_total`, `alerts_pending_total`, `telegram_delivery_latency_seconds`, `anomaly_explanation_latency_seconds`, `anomaly_explanation_failures_total`, `sentiment_pulse_duration_seconds`), Agent Gateway OTLP → Prometheus, **`prometheus-nats-exporter`** (official NATS exporter — expose `nats_consumer_pending_messages`, `nats_publish_errors_total`, `nats_consumer_redelivered_messages_total` per consumer group); add `prometheus-nats-exporter` service to `docker-compose.yml` alongside NATS
- [ ] T072 [P] Import Grafana dashboards: Go API latency (p50/p95/p99), NATS throughput, Agent Gateway LLM call latency + cost, signal evaluation cycle time; add Langfuse dashboard link (`http://localhost:3100`) to Grafana nav for LLM-specific observability (prompt/completion logs, per-user cost, prompt versioning); verify Langfuse shows unified traces from both ai-service (Python SDK path) and Agent Gateway (OTLP path) in the same project
- [ ] T073 [P] Add Playwright E2E test: full happy-path flow (register → link Telegram mock → create strategy → view history → add watchlist item) in `frontend/tests/e2e/happy_path.spec.ts`; MUST run at both desktop (1280×720) and mobile (375×667) viewports per SC-006
- [ ] T074 [P] Add Playwright accessibility audit (axe-core) on all pages; assert zero WCAG 2.1 AA violations in `frontend/tests/e2e/a11y.spec.ts`
- [ ] T075 Run Lighthouse CI on React SPA: assert LCP ≤ 2.5 s, CLS ≤ 0.1; add as CI step
- [ ] T076 [P] Verify all API endpoints respond < 200 ms p95 under 50-user simulated load using `k6` (or `hey`); **MUST also assert**: (a) signal evaluation loop peak memory < 256 MB (per NFR-PERF-003) via Go runtime `MemStats` or container `docker stats` during load test; (b) Agent Gateway digest pipeline completes within 5 min for 50 users × 10 projects (per NFR-PERF-004) — measured via Agent Gateway OTLP traces and Prometheus `agent_gateway_llm_call_duration_seconds` + `agent_gateway_message_send_duration_seconds` metrics, NOT Go pprof (the Agent Gateway is a third-party binary); Agent Gateway container memory < 512 MB measured via `docker stats` or cAdvisor; add results to `specs/001-investment-intel-poc/research.md`
- [ ] T077 [P] Final coverage sweep: assert `go test -cover` ≥ 80% globally; ≥ 95% on `domain/investment/signals/`, `domain/investment/alerts/`, `platform/auth/`, `platform/billing/`; fail CI if thresholds not met
- [ ] T078 [P] Final lint sweep: `golangci-lint run ./...`, `ruff check .`, `eslint .` — zero warnings; fix all or add justified inline suppressions
- [ ] T078a [P] Configure Prometheus alert rules in `infra/monitoring/alerts.yaml`: (1) `digest_delivery_failures_total > 0` within 1 h → page operator; (2) `signal_evaluation_cycle_seconds p95 > 25` (>83% of 30 s tick) → warn; (3) `telegram_delivery_latency_seconds p95 > 30` → warn; (4) `nats_consumer_pending_messages{consumer="alerts-dispatcher"} > 50` for 5 min → warn (stuck consumer); (5) `nats_consumer_pending_messages{consumer="alerts-redriver"} > 100` for 5 min → warn (re-drive backlog growing); (6) `nats_publish_errors_total > 0` → warn; (7) `anomaly_explanation_failures_total > 10` within 1 h → warn (explanation enrichment degraded); (8) `sentiment_pulse_duration_seconds p95 > 180` → warn (sentiment pulse exceeding 3 min SLA); wire all rules into Grafana alert notification channel

**Checkpoint**: All constitution quality gates pass in CI. Prometheus + Grafana operational. Performance budgets met.

---

## Phase 11: Deployment & POC Handoff

- [ ] T079 Write `infra/docker-compose.prod.yml` with production overrides: image tags pinned, env vars from secrets, resource limits — Agent Gateway service uses the GoClaw `latest` (or pinned `vX.Y.Z`) image only; **no separate `agent-gateway-web` container** (web UI is embedded in the binary since Apr 2026); use `latest-otel` variant if OpenTelemetry export is required; **NATS persistent volume**: NATS is configured with `FileStorage` (per T010b stream configs) — the production compose MUST mount a named volume for the NATS data directory (e.g., `nats-data:/data`) so that unprocessed `alert.pending` messages in the `ALERT_QUEUE` stream survive container restarts; verify this is also present in `docker-compose.yml` (local dev) to catch issues early
- [ ] T080 [P] Write `specs/001-investment-intel-poc/quickstart.md`: local dev setup (clone → `cp .env.example .env` → `docker compose up`) and production deploy steps
- [ ] T081 [P] Configure Cloudflare DNS + WAF rules; verify TLS termination via Traefik
- [ ] T082 [P] Smoke test production deployment: register a user, link Telegram, create strategy, add watchlist item, confirm digest arrives next morning
- [ ] T083 [P] Tag release `v0.1.0-poc` in Git; verify `CHANGELOG.md` has incremental entries from each feature PR (per T001a and constitution Quality Gates §6); consolidate any missing entries; final release entry under `## [v0.1.0-poc]` heading with date

**Checkpoint**: POC running on DigitalOcean. Smoke test passed. Handoff complete.

---

## Summary

| Phase | Stories | Tasks | Critical Path |
|-------|---------|-------|---------------|
| 1 – Setup | — | T001–T010c, T053a, T084–T086 (incl. T001a, T009a) | Blocks everything |
| 1b – LLM + Langfuse | — | T110–T122a | Blocks AI features |
| 2 – Foundation | US0 (auth) | T011–T018a (incl. T013a, T018a) | Blocks US1–US5 |
| 3 – Strategy Config | US1 (P1) | T019–T027, T087–T089 | MVP gate |
| 4 – Telegram Linking | US4 (P1) | T028–T034 | Blocks US2 + US3 delivery |
| 5 – Notifications | US2 (P2) | T035–T043b (incl. T042a, T043a, T043b) | Core value delivery |
| 6 – Alert History | US5 (P2) | T044–T047 (incl. T046a) | Pairs with US2 |
| 7 – Watchlist + Digest | US3 (P3) | T048–T059 (incl. T056a; excl. T053a moved to Phase 1) | Agent Gateway + AI layer |
| 7a – Anomaly + Sentiment | US6 (P3), US7 (P3) | T090–T105 (incl. T098a) | Depends on Phase 5 + 7 |
| 8 – Billing | US0 | T060–T066 | Revenue gate |
| 9 – Admin | US0 | T067–T070 | Ops tooling |
| 10 – Quality Gates | — | T071–T078a | Constitution compliance |
| 11 – Deploy | — | T079–T083 | POC handoff |

**Total tasks**: 129 (127 prior + 2 added by speckit.analyze remediation: T013a `app_projects` migration moved to Phase 2, T018a `PATCH /users/me` handler; T053a moved from Phase 7 to Phase 1; T042a redriver test path corrected; T040 indicator paths corrected to `domain/investment/indicators/`; T010a expanded with `POST /explain/{asset}` and internal endpoint contracts; T051 simplified to watchlist-only DDL; T083 CHANGELOG verification expanded) (110 prior + 17 added for Phase 7a: T090–T105 + T098a — Anomaly Explanation (US6) and Market Sentiment Pulse (US7); T038 expanded with `explanation TEXT` column for FR-032; T057a expanded with anomaly/sentiment agent endpoint grants; T071 expanded with anomaly/sentiment Prometheus metrics; T078a expanded with anomaly/sentiment alert rules; T001 expanded with `anomaly-agent/` and `sentiment-agent/` directories) (96 prior + 6 added by analysis remediation: T001a, T009a, T043b, T046a, T056a; T034/T035/T037/T040/T045/T073 expanded in-place; T020/T021/T025/T035/T039a/T040/T040a further expanded for volume-spike, MACD-crossover, and Bollinger-Band-breach signal types; T021/T025/T039/T051 updated for asset extensibility — `app_strategies.asset` changed from enum to VARCHAR FK to `app_projects`; adding new tokens is a data-only change, no schema migration required; T021/T025/T038/T039/T039a/T051/T053c updated for explicit USD quote-currency denomination — `app_projects.quote_currency` column added, market data adapters pass it as CoinGecko `vs_currencies` / CryptoCompare `tsym`, UI shows currency symbol on threshold inputs, alerts snapshot quote_currency at creation time; **speckit.analyze remediation**: T006 Docker networks, T010a ai-service contracts, T020/T022/T025 `is_signal_asset` validation, T021 `ON DELETE CASCADE`, T038 `ON DELETE SET NULL`, T039/T039a HTTP 429 handling, T053c DB-backed registry, T054 DuckDuckGo adapter, T076 memory + Agent Gateway performance assertions; **seed config + SDF**: T084–T086 seed YAML + schema + loader, T087–T089 SDF validation + import/export)

**Architecture review changes applied** (no new tasks; existing tasks expanded):
- **M2 — pct_change cache key fix**: T040 + T040a split `POST /pct_change/{asset}` into a separate endpoint with cache key `pct_change:{asset}:{window_minutes}` (avoids collision with indicator cache keyed by `candle_minutes`)
- **M4 — Circuit breaker**: T040a adds `sony/gobreaker` wrapping all ai-service HTTP calls; circuit opens after 5 failures, half-open probe after 30 s
- **M5 — Single Telegram bot**: T010, T030 clarified that `AGENT_GATEWAY_TELEGRAM_BOT_TOKEN = TELEGRAM_BOT_TOKEN` (one bot, Go owns webhook, the Agent Gateway only sends outbound); plan.md §Single Telegram Bot Architecture added
- **M6 — Graceful shutdown**: T041 adds `SIGTERM`/`SIGINT` handler: stop ticker → drain NATS → close Redis → close Postgres → shutdown HTTP (15 s deadline)
- **M7 — ai-service readiness gate**: T041 adds startup poll of `GET /health/ready` (2 s interval, 60 s timeout) before starting evaluation ticker
- **M3 — MACD pagination**: T039a mandates two paginated CryptoCompare calls for `candle_minutes=60` (2100 candles); 'accept 33 candles' alternative removed
- **M1 — Request budget**: plan.md §Market Data Feed now includes CryptoCompare request budget analysis with mitigation strategies
- **L2 — DB indexes**: T021, T038, T051 now include explicit index definitions per plan.md §Database Index Strategy
- **L3 — Migration ordering**: T051 makes `002a_projects.sql` placement in Phase 2 a CRITICAL requirement, not a note
- **L4 — Error response contract**: T010a requires standardised error envelope `{ error: { code, message, details? } }` per plan.md §Standardised Error Response Contract
- **P5 — Provider abstraction**: T001 creates provider subdirs (`auth/supabase/`, `billing/stripe/`, `notification/telegram/`, `eventbus/nats/`); T002 defines provider-agnostic interfaces (`AuthProvider`, `BillingProvider`, `Sender`, `AccountLinker`, `Publisher`, `Consumer`); T012 separates auth middleware from Supabase impl; T014 isolates NATS into `eventbus/nats/`; T015 isolates Stripe into `billing/stripe/`; T030/T031 implement Telegram as `Sender`/`AccountLinker`; T061/T062 isolate Stripe checkout/webhook into `billing/stripe/`; T063 uses `SubscriptionChecker` interface only; all provider-specific SDKs confined to their provider subdirectory — swapping Supabase→Casdoor, Stripe→LemonSqueezy, Telegram→Discord, or NATS→Kafka requires only a new provider subdir + one wiring change in `main.go`
